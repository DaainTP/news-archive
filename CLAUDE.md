# news-archive 작업 규칙

CM(건설사업관리) 제안서에 쓸 이슈를 수집·정리해 저장하는 저장소다.
정리된 텍스트는 통합포털에 그대로 노출되므로, 읽기 쉬운 평문으로 쓴다.

## 산출물

- `news/archive/YYYY-MM-DD.json` — 회차별 아카이브 (KST 기준 날짜)
- `news/latest.json` — 최신 회차와 동일한 내용 (포털이 읽는 파일)

JSON 구조:

```
{
  "date": "YYYY-MM-DD",
  "generated_at": "YYYY-MM-DDTHH:MM:SS+09:00",
  "no_news": false,
  "coverage": "YYYY-MM-DD ~ YYYY-MM-DD 보도분 (최근 1주일)",
  "items": [ { "title", "source", "url", "summary", "followup"(선택) } ],
  "trend_note": "..."
}
```

## 수집 규칙

1. **기간**: 최근 7일 이내 보도된 기사만 쓴다. 그 이전 기사는 아무리 내용이 좋아도 제외한다.
2. **날짜 검증 필수**: 검색 요약에 적힌 "9월 18일"류 표현을 그대로 믿지 않는다.
   - URL에 보도일자가 박힌 기사(`202609180734…`, `20260916001088`, `…/2026/09/18/…`)를 우선 채택한다.
   - 요일·연도가 어긋나면 과거 연도 기사다. (예: "9월 15일(월)"은 2026년이 아니라 2025년)
   - 날짜를 끝내 확정하지 못한 기사는 **쓰지 않는다.**
3. **중복 금지**: 저장 전 반드시 아래 점검을 돌린다.
   ```
   python3 tools/dedup_check.py news/archive/YYYY-MM-DD.json
   python3 tools/dedup_check.py --list 14        # 수집 전 최근 2주 수록 목록 확인
   python3 tools/dedup_check.py --title "제목" --url "URL"   # 후보 낱건 점검
   ```
   - `DUP`로 잡히면 **빼는 것이 기본**이다.
   - 이미 다룬 이슈라도 실질적 후속 진전(의결·시행·판결·사고 원인 규명 등)이 있으면,
     제목 앞에 `[후속]`을 붙이고 항목에 `"followup": true`를 넣어 남긴다.
     이때 요약 첫 줄에 "직전 수록: YYYY-MM-DD, 이후 달라진 점"을 적는다.
4. **건수를 억지로 채우지 않는다**: CM 전용 뉴스는 주당 몇 건 수준이다.
   새 이슈가 2건이면 2건만 저장한다. 없으면 `"no_news": true`로 두고 사유를 `trend_note`에 적는다.
   대신 범위는 CM 전용 기사에 한정하지 말고, 제안서에 쓸 수 있는 건설 전반
   (공사비·제도·안전사고·신기술/신공법·대형 국책사업 관리)까지 본다.

## 항목 작성 형식

`summary`는 아래 4개 절을 이 순서로 쓴다. 제목에는 `[공사비·제도]`, `[신기술]`,
`[신공법]`, `[사고·안전]`, `[사업관리]` 같은 분류 태그를 붙인다.

```
■ 이슈 (사실관계)
■ 문제점 (현장에서 터지는 지점)
■ CM단의 관리 / 해결방안
■ 제안서 활용 포인트
```

- 수치는 원문 기준으로 적고, 추정치는 추정이라고 밝힌다.
- 'CM단의 관리 / 해결방안'은 선언이 아니라 절차로 쓴다(누가, 언제, 무엇을 확인·기록하는지).

## trend_note에 반드시 남길 것

- 이번 회차의 관통 주제 요약
- **제외 항목과 사유** (기간 초과·연도 오인·중복 등). 어떤 기사를 왜 뺐는지 남겨야 다음 회차가 같은 실수를 반복하지 않는다.
- 확인 한계 (원문 접근 차단 등)

## 환경 제약

실행 환경의 네트워크 프록시가 국내 언론사·정부 사이트(molit.go.kr, cmak.or.kr,
edaily, koscaj 등)를 차단한다. WebFetch는 대부분 실패하므로 WebSearch 결과 요약을
근거로 쓰되, 위 2번(날짜 검증)을 반드시 적용한다.

## 브랜치

작업 브랜치는 `claude/dreamy-sagan-f4n0p5`, 푸시는 `git push -u origin claude/dreamy-sagan-f4n0p5`.
