# SketchUp 모델링 규칙 (PDF 도면 → 모델)

> 적용 범위: 사용자가 도면 PDF를 첨부하고 모델링을 요청했을 때만 적용한다.
> 뉴스 아카이브(`news/`) 작업에는 관여하지 않는다.

## 핵심 원칙

**전체 모델을 `DrawKit.build` 블록 하나로 만들어 1회 전달한다.** 부재별 왕복이 느림의 원인이다.

전달 경로는 둘 중 되는 쪽을 쓴다. 코드 내용은 동일하다.
- SketchUp MCP(`eval_ruby`)가 붙어 있으면 → 블록 전체를 **1회 호출**로 보낸다.
- 없으면 → 코드 블록을 그대로 주고 사용자가 Ruby 콘솔에 **붙여넣는다**. 파일 저장 불필요.

`drawkit.rb`는 **최초 1회만** 설치한다. 사용자가 폴더를 찾을 필요 없도록
`tools/sketchup/install_drawkit.rb`(자동 설치 스크립트)를 쓴다. 설치 절차는 `tools/sketchup/README.md`. 이후 매 작업에서 전달하는 것은 치수 블록뿐이다.

## 절차 (3단계)

1. **코드 생성** — PDF에서 부재를 읽어 바로 `DrawKit.build` 블록으로 쓴다.
   블록 첫 주석에 **기준면·원점·축척**을 명시한다. 치수표를 따로 만들어 승인받지 않는다.
   코드가 곧 치수표이고, 검증은 자동이다.
   **읽히지 않는 치수는 추측하지 말고, 그 부재만 빼고 만든 뒤 질문한다.**
2. **실행** — 사용자가 붙여넣거나 MCP로 1회 전송.
3. **결과 확인** — `검증 통과` 한 줄이면 끝. 불일치가 나오면 **해당 부재만** 다시 만드는
   짧은 블록을 낸다(전체 재실행 금지).

## 작성 규칙

```ruby
DrawKit.build("1층 골조") do          # 원점: 좌측하단 기둥심 / GL=0 / 단위 mm
  group "STR-1층" do                 # 부재 20개 넘으면 group 으로 묶는다
    box "C1-1", 0,    0, 0,    500,  500, 3000   # 이름, 원점X,Y,Z, 크기W,D,H
    box "G1-1", 500,  0, 2400, 4500, 400, 600
    cyl "PILE-1", 1000, 1000, -2000, 300, 2000   # 이름, 중심X,Y, 바닥Z, 반지름, 높이
    shape "L-1", [[0,0],[600,0],[600,200],[0,200]], 0, 3000  # 이름, 단면, 바닥Z, 높이
  end
end
```

- 모든 숫자는 **mm**. 생 숫자를 직접 좌표에 쓰지 않는다(SketchUp은 인치로 해석).
- 부재명은 **고유**해야 한다(C1-1, C1-2 …). 중복이면 실행 전에 차단된다.
- `.skp` 저장이 필요하면 `DrawKit.build("...", save_to: "C:/sketchup/이름.skp")` —
  검증을 통과했을 때만 저장된다.

## 하지 말 것

- 부재마다 개별 호출 — 반드시 한 블록에 담는다
- `DrawKit.build` 밖에서 형상 생성
- 도면에서 확인되지 않은 치수를 임의로 채워 넣기
- 불일치 1~2개 때문에 전체 블록 재실행

## drawkit.rb 를 수정했을 때

`ruby tools/sketchup/test/test_drawkit.rb` 와 `ruby tools/sketchup/test/test_installer.rb`
둘 다 전체 통과를 확인한 뒤 전달한다. `drawkit.rb` 를 고치면 `install_drawkit.rb` 안에
박혀 있는 사본도 함께 갱신해야 한다(`test_installer.rb` 가 원본과 동일한지 검사한다).
SketchUp 구버전(Ruby 2.x)에서도 돌아가야 하므로 endless method(`def f = ...`),
`tally`, `&.` 등 Ruby 3.x 전용 문법을 쓰지 않는다.
