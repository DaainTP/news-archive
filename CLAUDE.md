# news-archive — CM 제안서용 이슈 수집 규칙

이 저장소는 건설사업관리(CM) 제안서에 바로 쓸 이슈를 매일 정리해 통합포털에 노출하기 위한 아카이브다.
정기 실행(스케줄) 세션은 **작업 시작 전에 이 파일의 규칙을 먼저 적용한다.**

## 1. 수집 대상 (가장 중요)

- **최근 7일 이내에 보도·발표된 자료만 수집한다.** 기준일은 실행일(KST)이며, 7일을 넘는 자료는 아무리 내용이 좋아도 넣지 않는다.
- 각 항목은 **원문 보도일/발표일을 확인한 뒤** 제목 또는 `source`에 날짜를 명시한다. 날짜를 확인하지 못한 자료는 넣지 않는 것을 원칙으로 하고, 불가피하게 넣을 경우 `trend_note`에 "보도일 미확인"을 반드시 기재한다.
- **링크는 클릭했을 때 그 날짜의 기사/발표로 연결되어야 한다.** 정부 지침 자료실·법령 페이지·회사 소개 페이지처럼 게시 연도가 오래된 상시 페이지는 뉴스 항목의 `url`로 쓰지 않는다.
- 예외: **현재까지 유효하게 시행 중인 제도·지침**을 다루는 경우에는, 최근 7일 이내에 그 제도를 다룬 보도·개정 고시·발주청 공고가 있을 때만 그 최신 문서를 근거로 수집한다. 오래된 로드맵·지침 자체를 '소식'으로 올리지 않는다.
- 주제 축: ① 사건·사고(건설현장 재해, 붕괴, 침하 등) ② 신기술·신공법(스마트건설, AI·로봇, OSC/모듈러, 디지털 감리) ③ 제도·법령(건산법·건진법·건축물관리법·안전 관련 개정·발의) ④ 사업비·공기·분쟁(공사비 검증, 클레임, 판례).
- 국내 사례를 우선한다. 해외 사고는 국내 현장에 직접 적용 가능한 경우에만 넣고, 해외 사례임을 제목에 명시한다.
- 건설현장이 아닌 사고(공장·시설 설치작업 등)를 넣을 때는 제목과 본문에 그 사실을 명시한다.

## 2. 중복 배제

- `news/archive/*.json`의 **최근 7일치 항목 제목을 먼저 확인**하고, 이미 다룬 이슈는 제외한다.
- 같은 이슈의 후속 보도는 **새로운 사실(의결·시행·처분·추가 피해 등)이 있을 때만** 넣고, 무엇이 새로운지 첫 문단에 쓴다.

## 3. 항목 수와 서술 형식

- 회차당 3~5건. 기준을 만족하는 자료가 없으면 **억지로 채우지 않는다.** 0건이면 `no_news: true`로 저장하고 그 사유를 `trend_note`에 적는다.
- 각 항목의 `summary`는 아래 4단 구조를 그대로 유지한다(통합포털 표시용 평문, 마크다운 기호 사용 금지).

```
■ 이슈 (사실관계)
· 날짜·주체·수치를 포함한 사실만. 추측 금지.

■ 문제점
· 그 이슈가 현장에서 터지는 지점. 4개 내외.

■ CM단의 관리 / 해결방안
1) 실제 CM 업무 절차(계약검토·검측·계측·기성·안전관리계획)로 번역해 기술. 5~6개.

■ 제안서 활용 포인트
제안서 어느 절에 어떤 제목으로 넣을지, 어떤 표현이 설득력 있는지.
```

## 4. 저장 방식

- `news/archive/YYYY-MM-DD.json` 신규 생성 + `news/latest.json`에 동일 내용 복사(최신본 유지).
- 스키마: `date`, `generated_at`(KST, +09:00), `no_news`, `items[{title, source, url, summary}]`, `trend_note`.
- JSON은 `ensure_ascii=False`, 들여쓰기 2칸으로 저장한다.
- `trend_note`에는 회차 요약과 함께 **확인 한계(접속 차단, 날짜 미확인, 표기 상충)와 제외 사유**를 반드시 남긴다.

## 5. 실행 환경 제약

- 이 환경의 네트워크 프록시는 국내 언론사·정부 도메인(molit.go.kr, korea.kr, mt.co.kr, cnews.co.kr 등) 직접 접속을 차단한다. 원문 확인이 안 되면 검색 결과 요약을 근거로 쓰되, 그 사실을 `trend_note`에 명시한다.
- 검증되지 않은 고시 번호·시행일·통계 수치는 단정하지 않는다. "미확인, 원문 재확인 필요"로 표기한다.

## 6. 커밋·푸시

- 작업 브랜치: `claude/dreamy-sagan-6ksnj6` (지정 브랜치 외 푸시 금지).
- 커밋 메시지: `CM 제안서용 이슈 정리 YYYY-MM-DD (문제점 → CM단 관리/해결방안)` + 항목 요약 불릿.
- PR은 사용자가 명시적으로 요청할 때만 생성한다.
