# SketchUp 모델링 규칙 (PDF 도면 → .skp)

> 적용 범위: 사용자가 도면 PDF를 첨부하고 모델링을 요청했을 때만 적용한다.
> 뉴스 아카이브(`news/`) 작업에는 관여하지 않는다.

## 환경 (MCP 미사용)

- **SketchUp MCP 서버(포트 9876, 확장 `su_mcp`)는 쓰지 않는다.** 소켓 타임아웃 15초·부재별 왕복 호출 때문에 느리다.
- 대신 **실행 가능한 Ruby 스크립트 1개(`build_*.rb`)를 만들어 전달**하고, 사용자가 SketchUp Ruby 콘솔에서 `load` 한 번으로 전체를 생성한다.
- 공용 헬퍼는 `tools/sketchup/drawkit.rb`, 스크립트 뼈대는 `tools/sketchup/TEMPLATE_build.rb`.
- `.skp` 파일 자체는 SketchUp 실행 환경에서만 생성된다(비공개 바이너리 포맷). 이 저장소/세션에서 직접 .skp를 써낼 수는 없으므로, 산출물은 **스크립트 + 실행 안내**이며 .skp 저장은 스크립트의 `DrawKit.save_as` 또는 사용자의 `다른 이름으로 저장`으로 이뤄진다.

## PDF 도면을 받았을 때의 절차 (순서 고정)

1. **치수표 작성** — 모델링 전에 PDF에서 부재를 뽑아
   `ID / 부재명 / 원점 X,Y,Z(mm) / 크기 W×D×H(mm) / 비고` 표로 만들어 사용자에게 보여주고 확인받는다.
   축척·단위·기준면·원점 위치를 반드시 명시한다. **읽히지 않는 치수는 추측하지 말고 물어본다.**
2. **스크립트 생성** — 확정된 치수표를 그대로 옮긴 `build_<프로젝트명>.rb` 를 만든다.
   - 첫 줄에서 `drawkit.rb` 를 `load` 한다.
   - 전체 생성은 `DrawKit.build("...")` 블록(= `start_operation(name, true)`) 안에서 한 번에 수행한다. 실패 시 자동 `abort_operation`.
3. **검증 코드 포함** — 스크립트 마지막에 `DrawKit.verify(EXPECTED)` 를 두어, 실제 바운딩박스(mm)를 치수표와 한 줄씩 자동 대조하게 한다.
4. **실행 안내 + 결과 확인** — 사용자에게 파일 배치 위치와 `load` 한 줄을 알려주고,
   콘솔 출력(`검증 통과` 또는 불일치 목록)을 받아 **불일치 부재만** 수정한 패치 스크립트를 낸다.

## Ruby 작성 규칙

- 모든 좌표·치수는 **mm**로 쓰고 DrawKit을 통해 변환한다. 생 숫자는 인치로 해석된다.
- `pushpull` 직전에 `f.reverse! if f.normal.z < 0` (DrawKit 내부에서 처리됨).
- 모든 그룹에 `g.name` 을 부여한다. 이름이 곧 audit/verify 의 키다.
- 부재가 많으면 논리 단위(본체/서랍/하드웨어, 또는 층별/공종별)로 `DrawKit.assembly` 하위에 묶는다.
- 한 파일이 커지면 `build_1_구조.rb`, `build_2_마감.rb` 처럼 나누되, 각 파일은 독립 실행 가능해야 한다.

## 하지 말 것

- 치수표 확정 없이 PDF를 보며 즉흥 모델링
- 부재마다 개별 호출(MCP든 Ruby든) — 반드시 한 트랜잭션으로 일괄 생성
- `DrawKit.build`(= `start_operation`) 없이 대량 생성
- `audit` / `verify` 검증 생략
- 도면에서 확인되지 않은 치수를 임의로 채워 넣기
